/*// plugins/commands.js

console.log('✅ Plugin: commands.js cargado (Comandos Generales)');

export function handleGeneralCommands(conn, m, args) {
    if (m.text === '!menu') {
        conn.sendMessage(m.chat, { text: 'Menú de Vegeta-Bot-MB: !ig, !tiktok, !kick, !play, ! antilink, !on, !off.' }, { quoted: m });
    }
    // Añadir más comandos generales aquí (e.g., !status, !help)
}
*/